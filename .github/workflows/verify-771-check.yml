name: verify-771 check
on:
  push:
    branches: [ "ci/verify-771" ]
  workflow_dispatch:
jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
      - run: npm install --no-audit --no-fund
      - name: Typecheck
        run: |
          set +e
          npx tsc --noEmit > tsc.out 2>&1
          echo "TOTAL_TS_ERRORS=$(grep -cE 'error TS' tsc.out || echo 0)"
          echo "--- errors in src/wallets/adapters/XBullAdapter.ts ---"
          grep -E '^src/wallets/adapters/XBullAdapter\.ts' tsc.out || echo "(none)"
      - name: Unit tests
        if: always()
        run: npx vitest run test/xbullAdapter.version.test.ts
